import type { Strategy, SplunkEvent } from './strategy';
export declare class ExponentialBackoffStrategy implements Strategy {
    private isBackoffInProgress;
    private maxNumberOfRetries;
    private events;
    private pendingEvents;
    private exponentialBackoffLimit;
    private sendEvents;
    constructor({ sendEvents, exponentialBackoffLimit, maxNumberOfRetries, }: {
        sendEvents: (events: SplunkEvent[]) => Promise<void>;
        exponentialBackoffLimit?: number;
        maxNumberOfRetries?: number;
    });
    addEvent(event: SplunkEvent): void;
    flushEvents(): Promise<void>;
}
//# sourceMappingURL=exponentialBackoffStrategy.d.ts.map