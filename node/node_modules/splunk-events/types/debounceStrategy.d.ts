import type { SplunkEvent, Strategy } from './strategy';
import type { DebouncedFn } from './debounce';
export declare class DebounceStrategy implements Strategy {
    private pendingEvents;
    private events;
    private isSendingEvents;
    private flushPending;
    private autoRetryFlush;
    private sendEvents;
    flushEvents: DebouncedFn;
    constructor({ debounceTime, autoRetryFlush, sendEvents, }: {
        debounceTime?: number;
        autoRetryFlush: boolean;
        sendEvents: (events: SplunkEvent[]) => Promise<void>;
    });
    abort(): void;
    addEvent(event: SplunkEvent): void;
    private flushImpl;
}
//# sourceMappingURL=debounceStrategy.d.ts.map