import { IOContext, RequestTracingConfig } from '@vtex/api';
import { AuthMethod } from '../typings';
export declare const getRequestConfig: (context: IOContext, authMethod: AuthMethod, metric: string, tracingConfig?: RequestTracingConfig | undefined) => {
    headers: Headers;
    metric: string;
    tracing: {
        requestSpanNameSuffix: string;
    } | {
        rootSpan?: import("@vtex/api").Span | undefined;
        referenceType?: import("@vtex/api").SpanReferenceTypes | undefined;
        requestSpanNameSuffix: string;
    };
};
export interface Headers {
    [key: string]: string;
}
