import { InstanceOptions, IOContext, JanusClient, RequestTracingConfig } from '@vtex/api';
import { AuthMethod } from '../typings/tokens';
import { AffiliateInput } from '../typings/affiliate';
export declare class Affiliate extends JanusClient {
    constructor(ctx: IOContext, options?: InstanceOptions);
    registerAffiliate({ name, id, salesChannelId, searchEndpoint, followUpEmail }: AffiliateInput, authMethod?: AuthMethod, tracingConfig?: RequestTracingConfig): Promise<void>;
}
